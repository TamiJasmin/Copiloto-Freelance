import { Placeholder } from '@/components/ui/Placeholder';

// TODO(MVP-2): business_name, logo_url y payment_info (CBU/Alias/PayPal) + cerrar sesion.
export default function Settings() {
  return <Placeholder title="Mi negocio" hint="Nombre, logo y datos de cobro para los PDFs." />;
}
